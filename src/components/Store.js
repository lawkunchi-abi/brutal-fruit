import React, { Component } from 'react';
import Filter from '../components/store-locator/Filter.js';

export default class Store extends Component {
      render() {
            return (

                  <div>
                        <Filter/>
                  </div>
            );
      }
}
